Django football app to save your squads, players and matches.
========
##Requirements
* Python
* Django

## Usage

* Type `python manage.py runserver` to start local server
* Go to 'http://localhost:8000/squads/' in your browser
